<h2>Formulario curso<br /><small>con Symfony 3</small></h2>
{{ form_start(
      form,
      {
        'action':'',
        'method':'post'
      }
    )
}}
  {# Personalización del campo título #}
  <label for="curso_titulo">Nombre del curso</label>    {# Cambiamos el nombre del label que se despliega #}
  {{ form_errors( form .titulo ) }}
  {{ form_widget( form .titulo ) }}         {# Despliega el campo personalizado #}
{{ form_end( form ) }}                      {# Cierre del formulario con el nombre del Formulario se cierra #}
